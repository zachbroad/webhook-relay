root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/api ./cmd/api"
  bin = "./tmp/api"
  full_bin = "./tmp/api --worker"
  include_ext = ["go", "html"]
  exclude_dir = ["tmp", "bin", "charts", "migrations", "scripts"]
  delay = 500

[misc]
  clean_on_exit = true
