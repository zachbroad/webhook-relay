{{- if .Values.secret.create }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "nitrohook.fullname" . }}
  labels:
    {{- include "nitrohook.labels" . | nindent 4 }}
type: Opaque
data:
  DATABASE_URL: {{ include "nitrohook.databaseUrl" . | b64enc | quote }}
  REDIS_URL: {{ include "nitrohook.redisUrl" . | b64enc | quote }}
  POSTGRES_PASSWORD: {{ .Values.postgres.password | b64enc | quote }}
{{- end }}
